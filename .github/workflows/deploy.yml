name: 自动部署到服务器

on:
  push:
    branches: [main]
  workflow_dispatch: # 允许手动触发，增加控制

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production # 使用生产环境
    concurrency: production # 防止并发部署

    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 1 # 浅克隆，提高速度

      - name: 服务器执行拉取命令
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.USERNAME }}
          password: ${{ secrets.PASSWORD }}
          port: ${{ secrets.PORT }}
          script: |
            set -e  # 遇到错误立即退出
            cd /opt/1panel/apps/openresty/openresty/www/sites/wenjiexusite/index

            # 备份当前版本（可选）
            git stash push -m "backup-before-deploy-$(date +%Y%m%d_%H%M%S)" || true

            # 拉取最新代码
            git config --global --add safe.directory "$(pwd)"
            git fetch --all --depth=1
            git reset --hard origin/main
            git pull --depth=1

            echo "✅ 已成功拉取 main 分支最新内容"
            echo "🚀 部署完成时间: $(date)"
